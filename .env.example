# Copy to .env.local for local development
# isTesting: true = local backend (proxy), false = production
# Defaults: dev=true, prod=false. Override:
# REACT_APP_IS_TESTING=true
# REACT_APP_IS_TESTING=false
